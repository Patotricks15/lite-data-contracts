[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "lite-data-contracts"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
manifest-path = "Cargo.toml"
module-name = "lite_data_contracts"